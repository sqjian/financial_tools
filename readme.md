```sql
select
	*
from
	main.part_a
where
	统一仓库 = '河南新乡仓'
	AND 统一名称 = '徐福记卷心酥香草牛奶味袋装扫码专供@4(6x52g) CN'
	and 统一日期 = '45627.0'
```


```txt
甲方：过滤出单据号
乙方：客户订单号
```

```
统一仓库 -> 统一名称 -> 统一日期 -> 单据号：原因
统一仓库 -> 统一名称 -> 统一日期 -> 单据号：原因
```